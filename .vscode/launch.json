{
  "version": "0.2.0",
  "configurations": [
    // Main class configuration
    {
      "type": "scala",
      "request": "launch",
      // configuration name visible for the user
      "name": "Launch Main",
      // full name of the class to run
      "mainClass": "main.Main",
      // optional arguments for the main class
      "args": [
        "--datapath",
        "gs://arctic-pad-default/data.csv",
        "--features",
        "x_0,x_1,x_2,x_3,x_4",
        "--target",
        "y"
      ],
      // optional jvm properties to use
      "jvmOptions": []
    }
  ]
}
