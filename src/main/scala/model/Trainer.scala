package model

object Model {
  def train(Args: Array[String]): Unit = {
    println("Train Model Called...")
  }

  def score(Args: Array[String]): Unit = {
    println("Score Model Called...")
  }
}
